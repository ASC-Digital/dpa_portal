import React from "react";
import Loading from "react-fullscreen-loading";

const Loader = () => {
  return (
    <Loading loading background={"rgba(0, 0, 0, 0.6)"} loaderColor="#4e73df" />
  );
};

export default Loader;
